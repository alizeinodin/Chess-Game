# Legacy package folder

This folder previously held a partial CQtDeployer output (binary without bundled Qt libs).

**Use the new packaging instead:**

```bash
./scripts/build-linux.sh
./scripts/package-linux.sh
```

Output: `dist/Bu-Ali-Chess-linux.zip`
